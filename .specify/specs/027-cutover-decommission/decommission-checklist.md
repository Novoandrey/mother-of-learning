# decommission-checklist — spec-027 Phase L (US6)

Что выводим из эксплуатации и когда. **Vercel + staging — сейчас (на cutover).
managed Supabase — после грейс-периода (~1–2 нед).**

## Сейчас (на cutover, T023) — после зелёного smoke (T017)

### Vercel
- [ ] 🌐 Apex `theloopers.org` уже НЕ указывает на Vercel (сделано в T015).
- [ ] 🌐 Vercel: поставить production-деплой на паузу **или** удалить проект.
      _Совет: сначала **пауза** (не удаление) на время грейса — даёт быстрый
      возврат на Vercel при позднем форс-мажоре. Удалить — вместе с managed после
      грейса._
- [ ] 🌐 Подтвердить: `mother-of-learning.vercel.app` больше не обслуживает
      боевой трафик; игроки на `https://theloopers.org`.

### staging.theloopers.org (один env на боксе)
- [ ] 🌐 Dokploy: снять домен `staging.theloopers.org` с приложения (или
      убрать staging-деплой). Остаётся один env — боевой на apex.
- [ ] 🌐 Cloudflare: удалить/освободить A-запись `staging` (опционально; можно
      оставить запись, если домен больше ни на что не указывает).
- [ ] 🐧 self-hosted `.env`: `ADDITIONAL_REDIRECT_URLS` — убрать staging, оставить apex.

### Зафиксировать грейс
- [ ] **Дата старта грейса managed:** __________ (T023). Отсчёт ~1–2 недели до T025.

## После грейса (~1–2 нед, T025) — отложенный тик оператора

Предусловия перед гашением managed:
- [ ] self-hosted стабильно отработал как боевой весь грейс (нет откатов, ошибок).
- [ ] свежий R2-бэкоп self-hosted есть и проверен на restore (US5/T020).
- [ ] никаких незакрытых причин держать managed как эталон.

Гашение:
- [ ] 🌐 Managed Supabase: удалить проект (Dashboard) **или** понизить до паузы,
      затем удалить. _После удаления возврата на managed нет — это финальный шаг._
- [ ] 🌐 Vercel: если был на паузе — теперь удалить проект.
- [ ] 🐧 Подчистить временные артефакты миграции на боксе (`dump-026/`,
      `/opt/migrate-*`) если не нужны.
- [ ] 🤖 `NEXT.md`: эпик «Своя инфра» закрыт; managed погашен; прод = бокс +
      self-hosted. (если не сделано в T024 — дописать дату гашения)

## Итог эпика

После Vercel+staging (сейчас) и managed (после грейса) приложение **целиком на
собственном боксе + self-hosted Supabase**. Цель эпика (DevOps-навык: бэкапы,
падения, восстановление на своей инфре) достигнута.
