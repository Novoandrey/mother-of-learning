# Claude Design export — Mobile Ledger

Reference frames (PC home + launcher, Ledger main + record sheet, starter equip).
`Mobile Ledger.dc.html` is the Claude Design canvas export. Reference only —
not prod code; port under project tokens + AGENTS.md per epic process.
