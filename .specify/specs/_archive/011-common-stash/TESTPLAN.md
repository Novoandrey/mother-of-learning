# Test plan — spec-011 Общак (updated chat 44)

URL: https://mother-of-learning.vercel.app/c/mat-ucheniya/

Проходи сверху вниз. Каждый пункт — отмечай **PASS**/**FAIL/описание** прямо
в этом файле или кидай в чат. Проваленный шаг — на будущее в
`backlog.md` как `BUG-0NN`.

Обозначения:
- **US-N** = user story из `.specify/specs/011-common-stash/spec.md`
- **PC** = любой твой персонаж
- **gp** = суммарно золото (внутренний агрегат)

Что поменялось с первой версии плана (для контекста):
- Slice A (chat 42): универсальный ряд `<TransactionRow>` — one-line,
  цвет+prefix, actor → counterparty для переводов.
- Slice B (chat 43): stash page переехал на `<BalanceHero>` (без
  inline recent list) + `<StashPageTabs>` (Предметы / Лента).
- Filter bar (chat 43): свёрнут по умолчанию; активные фильтры как
  removable chips; метка текущей петли.
- Ownership guard (chat 43): нельзя «положить в общак» предмет,
  которого у PC нет.
- IDEA-043 (chat 44): на `/accounting` без pc-фильтра transfer
  рендерится одной строкой (sender leg).

---

## 0. Навигация и базовые точки входа

- [ ] **0.1** `/c/mat-ucheniya/accounting` — в хедере рядом с «Категории»
  есть кнопка `💰 Общак →`. Клик ведёт на `/accounting/stash`.
- [ ] **0.2** В левом сайдбаре внутри группы типов есть «ОБЩАК» → 1
  элемент. Клик по нему редиректит на `/accounting/stash` (а не
  на generic catalog page).
- [ ] **0.3** На странице любого PC над wallet-блоком («Кошелёк»)
  видны две кнопки: «Положить в Общак» / «Взять из Общака».
- [ ] **0.4** На `/accounting` dropdown «Актор» первой опцией
  показывает `💰 Общак`; рядом с [+ Доход][− Расход][Перевод]
  появляются «Положить в Общак» / «Взять из Общака» когда выбран
  **PC** (не Общак).

---

## 1. Стах-страница (US6) — новый layout после Slice B

- [ ] **1.1** `/c/mat-ucheniya/accounting/stash` рендерится без
  ошибки «нода не найдена».
- [ ] **1.2** Header: крупная иконка 💰, заголовок «Общак», под ним
  caption «Петля N» (текущая петля). Справа — кнопка «← Бухгалтерия».
- [ ] **1.3** Hero-карточка «Баланс общака»: крупное число gp,
  разбивка cp/sp/gp/pp, кнопка `+ Транзакция` сверху справа.
  **Inline recent list отсутствует** (переехал в таб).
- [ ] **1.4** Ниже — табы: «Предметы (N)» / «Лента транзакций».
  По умолчанию открыта «Предметы». Переключение моментальное (CSS,
  без server roundtrip).
- [ ] **1.5** Таб «Предметы»: если пусто — dashed-бокс с подсказкой;
  если непусто — строки (сворачиваемые по клику). Счётчик на табе
  соответствует количеству строк.
- [ ] **1.6** Таб «Лента транзакций»: те же ряды что на `/accounting`,
  но отфильтрованы по stash-ноде. В filter-bar **нет** группы
  «Персонажи» (actor зафиксирован). Summary «N транзакций» без
  «M персонажей» (всегда 1).
- [ ] **1.7** Клик «← Бухгалтерия» возвращает на `/accounting`.

---

## 2. Положить деньги в Общак (US1)

Предусловие: у PC на балансе ≥ 20 gp.

- [ ] **2.1** На странице PC жмёшь «Положить в Общак».
  Открывается sheet. В шапке чип `→ Общак`, подпись «Вы кладёте
  в общак». Таб-переключатель: `Деньги` / `Предмет`.
- [ ] **2.2** Ввести 20 gp, комментарий «тест-положить» → «Создать».
  Sheet закрывается, лента PC обновляется, у PC −20 gp.
- [ ] **2.3** В Общаке (`/accounting/stash`): баланс вырос на 20 gp.
- [ ] **2.4** В ленте бухгалтерии (`/accounting`) — **одна** строка
  transfer'а (после IDEA-043): `PC → Общак` с суммой `−20 GP` (red).
  На странице PC и в stash-tab — одна строка каждая (так было и до
  043, поскольку pc-фильтр уже отсекал sibling).

---

## 3. Взять деньги из Общака (US2)

Предусловие: в Общаке ≥ 5 gp (после теста 2 — будет).

- [ ] **3.1** На странице **другого** PC жмёшь «Взять из Общака».
  В шапке чип `← Общак`, подпись «Вы берёте из общака».
- [ ] **3.2** Ввести 5 gp, комментарий «тест-взять» → «Создать».
- [ ] **3.3** PC: баланс +5. Общак: баланс −5. На `/accounting`
  **одна** строка: `Общак → PC` с суммой `−5 GP` (sender = общак
  в этом направлении — т.е. строка выглядит так: actor_bit
  «Общак → PC», сумма красная отрицательная).

---

## 4. Shortfall-подсказка (US3 — три режима)

Предусловие: 3 PC на разных балансах. В Общаке разная сумма.

### 4.1 Режим «rich» (в Общаке больше, чем не хватает)
- [ ] **4.1.a** PC имеет 3 gp. В Общаке ≥ 100 gp. Открыть форму PC
  → вкладка «Расход» → 10 gp.
- [ ] **4.1.b** После ввода (секунду-две) появляется **жёлтая**
  плашка «Не хватает 7 gp — добрать из общака?» с кнопками
  [Да, добрать] / [Нет, уйти в минус].
- [ ] **4.1.c** Нажать [Да, добрать]. Плашка превращается в синий
  info-блок «При сохранении недостающее возьмём из общака»
  (есть ссылка «изменить»).
- [ ] **4.1.d** «Создать» → в ленте на `/accounting`: **две** строки
  (вместо старых трёх):
  - transfer: `Общак → PC` на −7 (sender leg)
  - expense: PC −10 (standalone)
  После: PC 0 gp, Общак −7 gp.

### 4.2 Режим «poor» (в Общаке меньше, чем не хватает)
- [ ] **4.2.a** PC имеет 2 gp. В Общаке 3 gp. Форма → расход 10 gp.
- [ ] **4.2.b** Плашка жёлтая, два уровня: «Не хватает 8 gp; в
  общаке только 3 gp» + «Добрать 3 gp и уйти в минус на 5 gp?».
- [ ] **4.2.c** [Да, добрать] → «Создать». Баланс PC: −5 gp (минус).
  Баланс Общак: 0 gp. В ленте те же две строки.

### 4.3 Режим «empty» (Общак пуст)
- [ ] **4.3.a** Общак = 0. PC имеет 2 gp. Форма → расход 10 gp.
- [ ] **4.3.b** Плашка **красная**: «Не хватает 8 gp; общак пуст» +
  «Сохранить расход — персонаж уйдёт в минус?». Кнопки
  [Да, сохранить] / [Отмена].
- [ ] **4.3.c** [Да, сохранить] → «Создать». PC баланс −8 gp.
  В ленте **одна** строка expense, без transfer'а.

### 4.4 Кнопка «Создать» блокируется до выбора
- [ ] **4.4.a** Пока плашка shortfall видна и ответ не выбран —
  кнопка «Создать» disabled, tooltip объясняет почему.
- [ ] **4.4.b** Если снизить сумму так что shortfall → 0, плашка
  исчезает, любой прошлый выбор сбрасывается.

### 4.5 Нет пинга при стах-пин режиме
- [ ] **4.5.a** «Взять из Общака» → 100 gp — **НЕТ** shortfall
  плашки, даже если в общаке нет 100. Обычная серверная ошибка
  (о недостаточности в общаке / сам процесс проходит валидно если
  просто заказать любую сумму).

---

## 5. Положить предмет в Общак (US4) — с ownership guard

- [ ] **5.1** PC → «Положить в Общак» → переключиться на «Предмет».
  Появляются поля: «Предмет» (текст) и «Количество» (число, по
  умолчанию 1).
- [ ] **5.2** **NEW (chat 43 ownership guard)**: ввести название
  предмета, которого у PC **нет**, например `super-cool amulet`,
  qty=2 → «Создать». Должна прилететь ошибка:
  `У персонажа недостаточно «super-cool amulet» — есть 0, нужно 2.
  Сначала запишите получение предмета отдельной транзакцией.`
  Транзакция **не** создаётся (проверить `/accounting`).
- [ ] **5.3** Сначала дать PC предмет «естественным путём»: форма PC
  → вкладка «Доход» (категория `loot`), **kind=item**, имя
  `silver amulet`, qty=3. Сохранить. В ленте — одна строка
  `kind=item` с `×3` (gray).
- [ ] **5.4** Теперь «Положить в Общак» → «Предмет» → `silver amulet`,
  qty=2 → «Создать». Успех. В ленте: **одна** строка (после 043):
  `PC → Общак  silver amulet  ×2`.
- [ ] **5.5** На `/accounting/stash` → таб «Предметы»: показывает
  «silver amulet — 2». При клике разворачивается, показывая
  «от: <имя PC>, день N».
- [ ] **5.6** Попробовать положить ещё 5 того же amulet'а — у PC
  осталось только 1. Должна прилететь ошибка про inventory.

---

## 6. Взять предмет из Общака (US5)

Предусловие: после US4 в Общаке 2 × silver amulet.

- [ ] **6.1** Другой PC → «Взять из Общака» → «Предмет» → введи
  `silver amulet`, qty=1 → «Создать». Успех (ownership guard
  проверяет stash-inventory тоже — в общаке есть 2, хватает).
- [ ] **6.2** В `/accounting/stash` → таб «Предметы»: silver amulet
  теперь 1 позиция.
- [ ] **6.3** В ленте — новая одна строка `Общак → PC  silver amulet
  ×1` (или `PC ← Общак` — см. фактический рендер; ожидаемое:
  одна строка вместо двух).
- [ ] **6.4** Попробовать взять 10 amulet'ов — в общаке только 1.
  Должна прилететь ошибка про stash inventory.

---

## 7. Wipe при переходе петли (US7)

**Требует DM прав** — нужно создать новую петлю.

- [ ] **7.1** В Общаке есть монеты и/или предметы в текущей петле.
- [ ] **7.2** Страница «Петли» → создать новую петлю, пометить
  `status=current`. Старую → `past`.
- [ ] **7.3** `/accounting/stash` теперь показывает баланс 0 и
  «пусто» в предметах (новая петля — новый срез). Filter-bar в
  stash-табе: в чипах «Петля» маркер `●` должен быть рядом с
  номером новой (текущей) петли.
- [ ] **7.4** В ленте `/accounting` с фильтром петли `N−1` старые
  стах-строки всё ещё видны (история не трётся — wipe чисто
  презентационный). Активный фильтр показывается как chip
  «Петля №(N−1)» в свёрнутой панели.

---

## 8. Фильтр ленты (US8 + новый filter UX)

- [ ] **8.1** `/accounting` — filter bar **свёрнут** по умолчанию.
  Видна только кнопка «Фильтры» (без счётчика, если ничего не
  выбрано).
- [ ] **8.2** Клик по «Фильтры» разворачивает панель с группами:
  Персонажи / Петля / День в петле / Категория / Тип. Клик по
  кнопке ещё раз — сворачивает.
- [ ] **8.3** В группе «Петля» рядом с номером текущей петли —
  маркер `●` (hover/title: «Текущая петля»).
- [ ] **8.4** Выбрать в «Персонажи» одного PC (например Мирияну).
  Свернуть панель. В свёрнутой шапке: кнопка «Фильтры 1», рядом
  chip с именем персонажа и `×` для удаления. Рядом ссылка
  «Сбросить всё». Лента отфильтрована.
- [ ] **8.5** Клик `×` на chip'е — фильтр удаляется, лента
  обновляется.
- [ ] **8.6** Выбрать текущую петлю — в свёрнутой шапке появляется
  chip `Петля №N · текущая`.

---

## 9. Edge cases

- [ ] **9.1** Нет текущей петли — на `/accounting/stash` баланс
  показывает lifetime-агрегат с подписью «За всё время», а
  предметы — dashed-бокс «Нет текущей петли — предметы не
  отображаются».
- [ ] **9.2** На странице PC когда нет current loop — кнопки
  «Положить/Взять из Общака» disabled с тултипом «Отметьте петлю
  как текущую, чтобы пользоваться общаком».
- [ ] **9.3** Редактирование существующей стах-транзакции (клик
  «изм.» на строке в stash tab «Лента») — открывает обычную форму
  (не stash-pinned), можно менять сумму/коммент.
- [ ] **9.4** Удаление transfer-пары (клик «уд.» на sender-строке
  в ленте) — спрашивает «Удалить перевод? Обе стороны будут
  удалены.», сносит обе legs из БД. На `/accounting` после refresh
  строка исчезает (обе legs пропали, показать было нечего).
- [ ] **9.5** Mobile (ширина <640px):
  - [ ] hero-карточка баланса вертикально (заголовок+баланс сверху,
        «+ Транзакция» снизу)
  - [ ] ряд транзакции переносится на 2 строки (основное + амоунт на
        первой, day chip + категория на второй — см. Slice A spec)
  - [ ] filter bar работает (collapsed-by-default одинаково с desktop)
- [ ] **9.6** Visual: цвета рядов — доход emerald-700, расход red-700,
  item gray-700. Контрасты достаточные (WCAG AAA по спецификации
  Slice A).

---

## Если всё зелёное

Отпиши в чат «все блоки зелёные» — тогда **T034 done**,
зовём следующий чат: spec-016 Clarify или другое из backlog.md.
