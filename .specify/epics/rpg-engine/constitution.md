# Эпик «RPG-движок» — конституция v1.5.0

**Создана**: 2026-06-12 (chat 95, по запросу Андрея) ·
**Применяется**: ко всем спекам эпика (044–052 и spec-022 v3); проверяется
на каждой фазе Specify/Plan каждой спеки эпика.
**Канон**: этот файл. Карта эпика и принципы живут здесь; `NEXT.md` и
spec.md спек держат копии/ссылки и синхронизируются при изменении.
**Иерархия**: дополняет конституцию проекта
(`.specify/memory/constitution.md`), не заменяет. При конфликте — проектная
старше, но конфликт = сигнал обсудить.

## Зачем эпик

Данные chat 90: у продукта есть поверхности данных, но нет поверхности
*игры* — 0 из 31 листа правились за месяц, чарники живут в таблице Стасяна.
Эпик переносит ежесессионный артефакт — чарлист — в продукт и строит под ним
универсальный механизм: персонаж = пирамида модулей, модуль декларирует, что
он делает. Одна система вместо хардкода категорий (спеллы/фиты/предметы/…).

## Карта эпика

| № | Спека | Суть | Зависит |
|---|---|---|---|
| **046** | Telegram Auth + Card v0 | identity-своп (initData→JWT на `auth.users`) + read-only карточка PC (имя + плейсхолдер); Telegram Mini App shell-pathfinder; держит вход для 044/022 | — (ПЕРВЫЙ шип) |
| **044** | Mobile Ledger | кошелёк/лента/общак/заявки поверх готовой бухгалтерии 009–019; несёт возврат Realtime | 046 (шелл/auth); параллельный трек |
| **045** | RPG Engine Core | модули + карточка модуля, эффекты, ресурсы, слой-0, прозрачность | — |
| 022 v3 | Player Mobile Mode | лист как поверхность игры; кубы; импорт xlsx | 045 P1; шелл/auth из 046 |
| 047 | База контента: спеллы | машинерия баз + dnd.su; поглощает roadmap-слот 039 | 045 |
| 048 | Форк нод | копия + `forked_from`; канон → хоумбрю | 045 |
| 049 | База контента: фиты + бэкграунды (+расы?) | повтор машинерии 047 | 047 |
| 050 | Пирамида прогрессии | таймлайн уровней ↔ модули; level-up руками | 045 |
| 051 | База классов/подклассов | таблицы прогрессии, точки выбора, скейлинг, слоты и мана-максимум (R12) | 050 |
| 052 | Конструктор хоумбрю | UI-редактор effect-блоков | 045, 048 |

Номера = стабильные ID, ≈ порядок исполнения; **источник истины по порядку —
строки этой карты + маркер «ПЕРВЫЙ шип»** (номер может отставать: 022 и 044
едут на шелле/входе из 046, у которого номер больше — Telegram-first пивот,
chat 96). Треки: **046 Telegram Auth+Card** — первый шип (Mini App shell +
identity-своп), даёт вход; **044 Mobile Ledger** — параллельный трек поверх
входа 046 (бухгалтерия 009–019 + возврат Realtime); критический путь листа
(mobile first): **045 P1 → 022 — второй шип**; всё дальше — обогащение.
Сопутствующий артефакт:
`specs/045-rpg-engine-core/research/coverage-checklist.md` — живая опись
свойств пилотов (Каэл, Миряна, Британия), не спека.

## Принципы

### E1. UX диктует данные

Каждая спека эпика проектируется от поверхности использования: сценарии
игрока → модель данных → код. Ни одна спека не шипится «только бэкендом» —
у движка тоже есть лицо (карточка модуля, добавление, разложение чисел).
Новые поверхности проходят design-проход до Plan (флоу spec-022/design.md).

### E2. Одна система

Раса, классовая фича, фит, предмет, спелл, бафф, факультатив, заметка —
один механизм «модуль». Никакого пер-категорийного хардкода; новая категория
контента = данные, не код (Конституция IX).

### E3. Текст — первоклассный гражданин

Имя + свободный текст + источник — полноценный модуль навсегда. Структура
(эффекты, ресурс) добавляется по желанию, никогда не требуется. Никаких стен
валидации; ничего не теряется молча. Сложное может остаться текстом навсегда.

### E4. Прозрачность по умолчанию

Любой участник кампании видит любой чарлист **целиком, всегда** и добывает
его легко (список всех PC — главная поверхность навигации, лист — в пару
тапов от входа). Тайн в чарлистах нет; тайны мира — отдельная история
(spec-033) и к модулям PC не относится. Правка — по-прежнему по правам
(owner/DM, `canEditNode`); прозрачность — про чтение.

Единственное решённое исключение (R9): **неопознанные предметы**. ДМ может
выдать предмет неопознанным — до опознания все, кроме ДМов, видят
маскированное имя («Неопознанные наручи», «Неопознанный меч»), без эффектов
и канон-текста; разложения чисел показывают вклад как «Неопознанный
источник». Флаг `identified` на attachment, P2.

### E5. Инспектируемость

Любой модуль — свой, чужой, канонный, форкнутый — открывается в карточку,
которая человеческим языком говорит, что он делает: текст, эффекты
(«+1 к КД», «преимущество на Скрытность»), ресурс с остатком, происхождение
(канон / форк-от / автор). Любое вычисленное число раскладывается на
слагаемые; каждое слагаемое ведёт в карточку модуля-источника.
«Почему КД 18?» — ответ в один тап из листа.

### E6. Добавление дешевле бумаги

Записать новое свойство/предмет/спелл — быстрее, чем карандашом на бумаге:
текстом, за секунды, с телефона, без обязательных полей кроме имени.
Прикрепить существующее (канон-предмет, спелл) — поиск + тап. Слежение там
же: остатки ресурсов видны и правятся в один тап везде, где модуль виден.

### E7. Одна правда, реалтайм у всех

Состояние листа живёт в одном месте, и правка видна всем **в реальном
времени**: открытые листы и карточки обновляются пушем, без действий
пользователя — игрок тратит Ци, ДМ видит это на лету. Ревалидация при
фокусе/реконнекте — слой устойчивости (мобильные сессии засыпают), а не
замена пуша. Конфликты (R11): play-state меняется **атомарными дельтами**
с зажимом 0..max — конкурентные траты/урон не теряются конструктивно;
last-write-wins остаётся только для замен (текст, ручная правка ✎, выборы)
с честным откатом-тостом, не тихим мержем. Таблица Стасяна — стартовая точка через импорт (one-way);
после переезда PC источник правды — приложение, параллельного канала нет.

*Инфра-пререквизит*: Realtime-сервис был вырезан из самохост-стека как
неиспользуемый (spec-024/T009); возвращение (контейнер + websocket-маршрут
+ авторизация каналов) — часть Plan spec-045, проверяется на staging.

### E8. Канон прибит, хоумбрю — через форк

Канон-ноды read-only для всех, кроме owner (правка owner'ом — in-place, без
версий, v1). Изменить канон под себя = форк: полная копия с `forked_from`,
апстрим не тянется. Хоумбрю — норма, не исключение («Ильза», «ГМ дал»,
обрезанные фиты факультативов).

### E9. Слой-0 — формулы Стасяна

Базовая деривация = канон таблицы (`memory/character-sheet-excel-system.md`):
мод, БМ, навыки, спасы, DC. Эффекты складываются поверх в детерминированном,
задокументированном порядке; ручное ✎ — абсолютно и обратимо. Паритет с
таблицей — пол, не потолок (Конституция VII, правило переноса).

### E10. Mobile first; шип игрокам — мерило

Каждая спека эпика обязана быть полезной на ближайшей сессии (Конституция
VII). Порядок работ минимизирует время до листа в руках игроков; движок
режется ярусами ровно под это (P1 — то, без чего лист не живёт).

### E11. Ratchet против усложнения

Словарь операций и целей эффектов растёт только от реальных свойств:
новый op — при ≥ 3 строках в coverage-checklist. Урок DiceCloud: настройка
не должна стать дороже бумаги. Сомнение → текст (E3), не схема.

## Зафиксированные решения

| # | Решение | Когда |
|---|---|---|
| R1 | Модуль = нода, единообразно; без inline-строк | chat 95 |
| R2 | Type/instance split: шаблон на ноде, инстанс (uses, оверрайды, уровень) на attachment | chat 95 |
| R3 | Форк = полная копия, `forked_from`, апстрим не тянется | chat 95 |
| R4 | Канон immutable для не-owner; owner — in-place без версий (v1) | chat 95 |
| R5 | `grants`-ребро: модуль выдаёт модули (раса → трейты; факультатив → обрезанный форк фита) | chat 95 |
| R6 | **Прозрачность: все читают все листы и все модули; правка по правам** | chat 95 (это ревью) |
| R7 | MVP модуля = текст (`effects: []`); слой-0 = формулы Стасяна; ops ярусами | chat 93 |
| R8 | Mobile first; критический путь 045 P1 → 022 | chat 93/95 |
| R9 | Неопознанные предметы: флаг `identified` на attachment, маскировка имени/эффектов/текста для всех, кроме ДМов (P2) | chat 95 |
| R10 | (заменён v1.6.0) Mobile Ledger (044) — параллельный трек поверх входа 046, несёт возврат Realtime; shell/auth-pathfinder = 046 (Telegram Mini App), не PWA | chat 95 / 96 |
| R11 | Свёртка эффектов: внутри op — таймстамп прикрепления, поздний `set`/`override` побеждает с показом затенённых; add→mult (хейст-аргумент); резерв `upgrade`/`downgrade`; roll-эффекты — отдельное пространство целей `roll.*`; play-state — атомарные дельты, LWW только для замен (пакет D-1А/2А/3А/4А/6А) | chat 95 |
| R12 | **Мана-система кампании**: пул маны = Σ(ячейки × стоимость уровня); касты ≤5 круга тратят только ману без лимита ячеек; касты ≥6 круга требуют ячейку своего круга И тратят ману (составная атомарная трата); восстановление — по recharge-флагам/долгий отдых. Правило — конфигурируемые данные кампании (стандартные слоты = другой конфиг); таблица уровень→мана = DMG Spell Points: 1→2, 2→3, 3→5, 4→6, 5→7, 6→9, 7→10, 8→11, 9→13 (подтверждено chat 95) | chat 95 |

## Процесс

- Specify каждой спеки эпика обязан содержать UX-сценарии своих поверхностей
  (E1); «UI потом» — не статус.
- Новые поверхности → design-проход между Clarify и Plan, **два артефакта**:
  1. компактный `design.md` (флоу spec-022) — принципы, анатомия экранов,
     состояния, токены; существующие паттерны переиспользуются;
  2. `design/prompt.md` — самодостаточный промпт для **Claude Design**,
     дистиллирующий design.md в задание «нарисуй экраны»: контекст продукта,
     тема/токены, список экранов с состояниями, по одному брифу на экран.
  Андрей прогоняет промпт в Claude Design (🌐), экспорт (JSX-фреймы / CSS /
  скрины) кладётся в `design/export/` папки спеки и служит референсом для
  Plan/Implement (прецедент — дизайн-пак spec-021: SPEC.md, HANDOFF.md,
  JSX, editor.css). Экспорт — референс, не прод-код: переносится осознанно,
  под токены проекта и правила `AGENTS.md`.
- Каждая контентная спека (046/048/050) обновляет coverage-checklist и
  отмечает закрытые строки.
- Изменение этой конституции — явное решение Андрея; бамп версии + строка в
  changelog ниже; копии (NEXT, spec headers) синхронизируются в том же
  коммите.

## Changelog

- v1.6.0 — 2026-06-20 (chat 96): Telegram-first пивот (решение Андрея). Вставлен
  **046 Telegram Auth + Card v0** как первый шип и shell/auth-pathfinder
  (Mini App, identity-своп `initData`→JWT на `auth.users`). 044 Mobile Ledger →
  параллельный трек поверх входа 046; контентный хвост сдвинут +1 (спеллы 047 …
  хоумбрю 052). R10 заменён: pathfinder теперь 046 (Telegram Mini App), не 044
  (PWA). Инвариант смягчён: номера = стабильные ID, порядок исполнения — по карте
  + маркеру «первый шип» (022 уже был контрпримером). Папки 044/045 не тронуты.
- v1.5.0 — 2026-06-12 (chat 95): карта перенумерована по порядку исполнения
  (решение Андрея): ledger 051→**044**, движок 044→045, дальше +1
  (спеллы 046, форк 047, фиты 048, пирамида 049, классы 050, конструктор
  051); R12 — таблица DMG Spell Points вписана как подтверждённая.

- v1.4.0 — 2026-06-12 (chat 95): R11 — пакет решений по свёртке
  (D-1А/2А/3А/4А/6А, см. research/best-practices-review.md §5); R12 —
  мана-система кампании как канон-вход движка (FR-025 spec-045); E7
  уточнён атомарными дельтами; карта 050 + мана-максимум.

- v1.3.0 — 2026-06-12 (chat 95): R9 — неопознанные предметы как исключение
  E4 (решение Андрея по D-15); R10 + строка карты — spec-044 Mobile Ledger,
  параллельный трек и кандидат на первый шип.

- v1.2.0 — 2026-06-12 (chat 95): §Процесс — design-проход даёт два
  артефакта: design.md + `design/prompt.md` для Claude Design; экспорт
  экранов → `design/export/` спеки, референс для Plan/Implement.

- v1.1.0 — 2026-06-12 (chat 95): E7 переписан — реалтайм-пуш у всех это
  принцип, не отложенный слой (уточнение Андрея «синхронизация = реалтайм»);
  FR-024/SC-009 spec-045 синхронизированы; инфра-пререквизит назван.
- v1.0.0 — 2026-06-12 (chat 95): создана по итогам ревью spec-045
  (UI/UX возвращён в движок; прозрачность R6; принципы E1–E11).
