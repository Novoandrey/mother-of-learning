# [draft] Стек

> Заглушка. Содержание будет наполняться постепенно.

Next.js 16 (App Router) + Supabase (Postgres + Auth + RLS) + Tailwind v4 + TypeScript strict + Vercel прод. vitest для pure-helpers. lucide-react для иконок, Manrope variable + JetBrains Mono variable шрифты. Каверзы Next.js 16 (например, `middleware.ts` → `proxy.ts`) — отдельная заметка в `mat-ucheniya/AGENTS.md`.

## Что планируется в статье

- Версии зависимостей и почему именно эти
- Структура папок: `app/`, `components/`, `lib/`, `scripts/`, `docs/`
- Server actions vs route handlers vs RPC — когда что
- Особенности Next.js 16, на которые наступаем
- Vercel deploy: что в проде, как пробрасываются env
